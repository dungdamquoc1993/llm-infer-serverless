# Tải model (download_model.py) — chỉ cần nếu repo HF private
# HF_TOKEN=

# Tuỳ chọn: bật auth cho vLLM (client gửi header Authorization: Bearer ...)
# Đặt trên RunPod Pod → Environment, hoặc docker run -e
# VLLM_API_KEY=
